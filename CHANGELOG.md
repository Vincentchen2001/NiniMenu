# 更新日志

## v0.8.0 - 周菜单变成"有记忆的"

### 新增
- **每周菜单永久存档**：周菜单不再是一次性缓存——每周一份独立保存，下周自动开新篇，上周的菜单原样留存。
- **历史页能看"当天排了什么"**：历史日历选中某天，会显示那天当时排的菜单，和实际打卡对照，排了没吃一眼看出。
- **手动加的菜不再被冲掉**：自己加进菜单的菜，点"重新生成"时会留在原位，系统只重排它自己排的部分。
- **"本周想吃 / 每天主题"只管这一周**：下周自动清空，不再永久残留到每个周三都想吃牛肉。

### 修复
- **重新生成不再改写过去**：周中点"重新生成整周"只重排今天及以后；昨天以前排过什么保持原样，推荐历史不再被悄悄顶掉。过去的天也不能再被单天重生成。
- **手动编辑保存此前实际是坏的**：菜品 JSON 只有序列化没有反序列化，前端"保存菜单"发回的数据后端读不懂；本次补齐反序列化后，手动加菜/删菜的保存真正生效。

## v0.7.2 (2026-06-11)

### 修复

- **汤的识别大幅收紧** — 之前做法步骤里出现「汤汁」、标签带「汤品」、名字带「煲」都会被当成汤，全库 632 道菜里 198 道被标成汤（红烧排骨、水煮牛肉、酸汤鱼、汤圆都在内）。现在只认两条：分类带「汤」，或菜名以「汤/羹」结尾；重启后全库自动重新标注（手动改过角色的菜不动），汤位只会排真正的汤，红烧排骨这类硬菜回归荤位竞争

### 新增

- **费时汤一周最多两道（规则 seed v4）** — 一周菜单里炖煮超过 45 分钟的汤排满两道后，第三道起 -25 分；与「工作日少排费时汤」叠加，等于工作日压制、周末两天各一道大炖汤封顶。规则页可调次数与分值
- **今天的汤不和昨天撞主料** — 排汤位时与昨天的汤食材重叠（比如连着两天鲫鱼汤）-18 分，葱姜蒜等基础配料不算；单日重新生成时会同时避开前后两天的汤。规则页可调分值
- **「最多出现」类规则的分值也能调了** — 限次类预设规则（辣菜不扎堆、费时菜分散、油炸限两次等）的「降多少分」步进器补齐

## v0.7.1 (2026-06-11)

### 新增

- **会做感知推荐（规则 seed v3）** — 收藏=会做：未收藏的菜 -12 分，一道收藏都没有的菜系再 -25 分；收藏过的菜不受影响，陌生菜系只在候选不足时偶尔出现（留学新菜的口子）。明日推荐同步同样降权
- **工作日少排费时汤** — 周一到周五汤位对炖煮超过 45 分钟或难度 hard 的汤 -30 分，周末不罚；三条新规则在规则页可见、可调分值、可单独关闭（调整作用于一周菜单，明日推荐使用相同的默认降权），迁移不影响你改过或关掉的旧规则
- **规则卡片分值步进器** — 优先安排/尽量避开类预设规则新增「加/降多少分」调节

### 优化

- **浓汤不再算清淡** — 清淡主题只接受清汤：汤品类且口味不带「浓」且 richness ≤ 1，猪肚鸡汤这类浓香炖汤不再混进清淡日，汤的清淡加分同步收紧

## v0.7.0 (2026-06-10)

### 新增

- **每日口味主题** — 一周菜单页新增主题条，每天可单独设置 均衡/快手/清淡/想吃辣/收藏/靓汤，按周重复生效；靓汤主题在没配汤位时自动加一道汤（`preferences.days`）
- **想吃什么就多排什么** — 周级"本周想多吃"与天级"这天想吃"食材偏好（猪/牛/羊/鸡鸭/鱼虾/蛋/豆制品），天级 +25 分、周级 +12 分参与排序
- **组合一键套用** — 工作日包（上班族快手/家常有节奏/清爽减脂/解馋重口）× 周末包（亲子时光/犒劳自己/靓汤滋补/简单休息），套用后预览整周主题
- **单日重新生成** — `POST /week-plan/regenerate-day`，只换某一天的菜，其余六天保持不变且不与其重复
- **家庭忌口** — `blocked_ingredients` 设置，命中菜名/食材/调料的菜在一周菜单、今晚吃什么、明日推荐里全部排除
- **人话规则卡片** — 预设规则以中文句子展示（开关 + 数量步进器），新增"一句话造规则"构建器；规则模板（`template` 字段）由后端渲染为表达式执行
- **每餐数量快捷预设** — 二人简餐/三口之家/人多聚餐 一键填入 12 个数量步进器

### 优化

- **默认规则集重做（seed v2）** — 保留蛋类/凉菜/主食硬限制与同餐蛋白、重辣降分；删除 4 条画像加分与冷热搭配（画像由生成器直接计分）；新增 辣菜不扎堆/费时菜分散/一周蛋白多样/一周油炸限两次；迁移只替换出厂原文，用户改过的规则原样保留
- **规则引擎** — 新增 `countOverlapDay/Week` 帮助函数与 `candidate.ingredients` 环境字段
- **修复推荐去重失效** — GORM `Pluck` 会重置目标切片，导致已吃记录被推荐记录覆盖；冷却窗口内的菜可能被重复推荐
- **周菜单概览徽章** — 顶部卡片的 总菜数/跳过的餐/状态 三格改为 荤/素/汤/收藏 徽章行，某类为 0 或收藏少于 3 道时变橙提醒；状态收为行尾小胶囊
- **修复选菜弹窗只显示前 100 道** — 菜品接口支持 `pageSize=0` 取全量；一周菜单/明日推荐选菜弹窗与菜品详情同类菜改为取全部启用菜，分类胶囊补全并换行排列，菜图改为懒加载
- **审查修复：忌口全覆盖** — 心情推荐的快捷分支与盲盒此前绕过忌口过滤已补齐；忌口设置卡此前读不出已保存词条、再次添加会覆盖旧词（数据丢失）已修复
- **审查修复：单日重生成** — 改完日主题立刻"仅这天重生成"不再重排整周（偏好保存不再清菜单缓存）；重生成前先保存手动换过的菜；并清掉该天已不适用的旧提示
- **审查修复：规则引擎** — 周范围规则不再把当天午餐算两次（上限会提前触发）；靓汤日不再每次误报"放宽口味要求"；预设规则的数量调整现在能正常保存；专家模式手改表达式自动脱离句子模板而不是被静默覆盖
- **菜品页桌面居中** — 菜品页此前是唯一没有 640px 居中容器的页面（初版外卖式全宽布局），桌面上分类栏贴边、右侧大片留白；现与其他页面一致居中，手机布局不变

## v0.6.0 (2026-06-05)

### 新增

- **上传图片自动压缩** — 手机上传的 PNG 截图/照片自动备份原图到 `uploads_backup/`，然后 EXIF 方向修正 + 缩放 + 转 JPG（quality=85），体积减少 70-90%
- **`internal/imaging` 包** — Go 图片处理核心（AutoOrientation + Lanczos 缩放 + 白底合成 + JPG 编码），依赖 `github.com/disintegration/imaging`
- **上传错误具体提示** — 前端上传失败时展示后端返回的具体原因（如"图片大小不能超过5MB"），替代通用"上传失败"文案
- **`getUploadErrorMessage`** — `api/client.ts` 新增工具函数，从 axios error 中提取后端 `message` 字段

### 优化

- **上传大小限制可配置** — `MAX_UPLOAD_SIZE_MB` 环境变量（默认 5MB），不再硬编码
- **Gin Multipart 内存限制** — `r.MaxMultipartMemory` 按上传大小配置动态设置，确保大文件请求到达 handler 校验
- **错误提示动态化** — 大小超限提示改为 `fmt.Sprintf("图片大小不能超过%dMB", maxMB)`，跟随配置值

### 新增环境变量

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `BACKUP_DIR` | `uploads_backup` | 上传原图备份目录 |
| `MAX_UPLOAD_SIZE_MB` | `5` | 上传大小上限 MB |
| `COMPRESS_MAX_DIM` | `1200` | 压缩最大宽高 px |
| `JPEG_QUALITY` | `85` | JPG 压缩质量 |

---

## v0.5.0 (2026-06-05)

### 新增

- **明日菜单偏好推荐** — `/pick/tomorrow` 接口，支持快速/清淡/辣/收藏/均衡偏好过滤 + 评分排序
- **应用名称自定义** — `/app-info` 接口，管理后台设置页可自定义应用显示名称
- **菜品图片字段** — RecipeSpec 新增 Image 字段，所有菜谱配置图片路径
- **imgtool 图片工具** — `backend/imgtool.exe`，支持白边去除（泛洪填充变透明）、自动裁剪、多尺寸图标生成（32/64/128/180/256/512）
- **语录管理页面** — 管理后台新增语录 CRUD 页面
- **记录管理页面** — 管理后台新增用餐记录查看页面
- **PWA 支持** — 新增 `manifest.json` 和多尺寸应用图标
- **成就解锁动画** — 前端成就解锁时显示 Overlay 动效
- **PageHeader 通用组件** — 子页面标题栏组件
- **成就进度展示** — 成就页显示进度条、解锁时间、自动/手动标签

### 优化

- **数据库性能** — SQLite WAL 模式 + 复合索引优化（pick/record/shopping）
- **成就系统防抖** — 批量处理成就检测，GROUP BY 统计事件
- **Dashboard 增强** — 分类分布、热门菜品、周趋势、难度分布统计
- **菜品管理** — 支持搜索、筛选、分页、批量启禁用/删除/改分类
- **菜品编辑重构** — 分区布局，多图上传、标签、排序权重
- **菜品详情** — 记录统计、多图画廊展示
- **History 页面** — 增强菜品图片/emoji 显示
- **Settings 重构** — Toggle/SettingRow 通用组件
- **Tomorrow 页面重构** — 偏好选择、目标道数、换菜/替换单道
- **应用图标更新** — 新版图标，旧图清理
- **记录接口** — 返回菜品图片 URL 和 emoji

### 修复

- `dayRatingApi.create` mood 字段改为可选

### 其他

- 新增 `.env` 加载支持
- 新增 `.gitignore`，排除构建产物和数据库
- 新增 Python 图片工具脚本（批量生成、压缩、格式转换）
- 新增 `pyproject.toml` 管理脚本依赖
- 新增 Windows/Linux 构建脚本

---

## v0.4.0 (2026-06-02)

### 新增

- **自动成就系统** — 80+ 成就按用餐/心情/推荐/照片/收藏/购物等维度自动解锁
- **AchievementEvent 模型** — 记录推荐/盲盒/周计划等事件触发
- **购物清单分类优化** — 精确词条匹配优先 + 关键词兜底，新增配料/其他分类
- **成就目录** — 从 database seed 迁移至独立包统一管理

---

## v0.3.0

### 新增

- 初始功能：智能推荐、食谱管理、周计划、购物清单、用餐记录、照片墙、管理后台

---

## v0.1.0

- 项目上传，基础框架搭建
